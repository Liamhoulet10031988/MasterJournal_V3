# Master Journal V2 - Обновление с новыми возможностями

## 📋 Реализованные функции

### 1. ✅ Расширение модели заказа
- **Новые поля**:
  - `workAmount` - сумма за работу
  - `ourParts` - описание наших деталей
  - `ourPartsAmount` - сумма наших деталей
  - `clientParts` - описание деталей клиента
  - `totalAmount` - итоговая сумма (workAmount + ourPartsAmount)

- **Автоматическая миграция**: Старые данные автоматически мигрируются при первом запуске:
  - `amount` → `workAmount`
  - `parts` → `clientParts`
  - `ourPartsAmount` = 0
  - `totalAmount` = workAmount + ourPartsAmount

### 2. ✅ История с календарем
- Интеграция библиотеки `react-native-calendars`
- Календарь с отметками дней, где есть заказы
- Переключение между режимом списка и календаря
- При выборе дня показываются заказы за этот день
- Навигация по месяцам и годам

### 3. ✅ Поиск по датам
- Фильтр по диапазону дат (startDate - endDate)
- Формат дат: YYYY-MM-DD
- Индикатор активного фильтра
- Кнопка сброса фильтра

### 4. ✅ Редактирование заказов
- Кнопка "Изменить" в деталях заказа
- Переиспользование компонента OrderForm для редактирования
- Функция `updateOrder(id, patch)` с валидацией
- Автоматический пересчет totalAmount

### 5. ✅ Обновленный экспорт
**CSV:**
- Убрано поле `id`
- Новые колонки:
  - Дата
  - День недели
  - Клиент
  - Авто
  - Работы (описание)
  - Сумма работы
  - Детали наши
  - Сумма деталей наших
  - Детали клиента
  - Оплата (Нал/Безнал/Долг)
  - Итого
  - Фреон (г)
  - Комментарий
- Итоговые суммы внизу:
  - По типам оплаты
  - Раздельно "Работа" и "Наши детали"
- UTF-8 BOM для корректного открытия в Excel/Android

**JSON:**
- Стандартный JSON (без id)
- Русский JSON с русскими ключами

### 6. ✅ Улучшенная статистика
- Общая сумма
- Разбивка по "Работа" и "Наши детали" с процентами
- Статистика по типам оплаты с дополнительной разбивкой

### 7. ✅ Универсальный компонент формы
- `OrderForm` используется для создания и редактирования
- Разделенные поля для работы и деталей
- Автоматический расчет итоговой суммы
- Подсказки для клиентов и авто

---

## 🚀 Запуск проекта

```bash
# Установка зависимостей (если еще не установлены)
cd /home/claude/MasterJournalV2-main
npm install

# Запуск приложения
npx expo start

# Для Android
npx expo start --android

# Для iOS
npx expo start --ios

# Для Web
npx expo start --web
```

---

## 📝 Чек-лист приемки

### Миграция данных
- [ ] При первом запуске старые данные автоматически мигрируются
- [ ] Старое поле `amount` преобразуется в `workAmount`
- [ ] Старое поле `parts` преобразуется в `clientParts`
- [ ] `totalAmount` рассчитывается корректно
- [ ] Совместимость со старым кодом сохранена

### Создание заказа
- [ ] Форма содержит все новые поля
- [ ] Поле "Сумма работы" обязательно
- [ ] Можно указать "Наши детали" и их сумму
- [ ] Можно указать "Детали клиента"
- [ ] Итоговая сумма рассчитывается автоматически (workAmount + ourPartsAmount)
- [ ] Быстрые работы заполняют только "Сумма работы"

### История с календарем
- [ ] Кнопка переключения между списком и календарем работает
- [ ] В календаре отмечены дни с заказами точками
- [ ] При клике на день отображаются заказы за этот день
- [ ] Можно листать месяцы вперед/назад
- [ ] Календарь на русском языке

### Фильтрация по датам
- [ ] Кнопка фильтра по датам открывает модальное окно
- [ ] Можно ввести начальную и конечную дату
- [ ] Кнопка "Применить" фильтрует заказы
- [ ] Показывается индикатор активного фильтра
- [ ] Кнопка "Сбросить" очищает фильтр

### Редактирование заказов
- [ ] В деталях заказа есть кнопка "Изменить"
- [ ] Открывается форма редактирования с заполненными данными
- [ ] Можно изменить все поля
- [ ] Изменения сохраняются
- [ ] totalAmount пересчитывается при изменении workAmount или ourPartsAmount
- [ ] При редактировании заказа-долга обновляется связанный долг

### Детали заказа
- [ ] Отображается "Сумма работы"
- [ ] Отображается "Детали наши" (если заполнено)
- [ ] Отображается "Сумма деталей наших" (если > 0)
- [ ] Отображается "Детали клиента" (если заполнено)
- [ ] Отображается "Итого" (totalAmount)
- [ ] Разделители между секциями

### Экспорт CSV
- [ ] CSV экспортируется с BOM (UTF-8)
- [ ] Заголовки на русском языке
- [ ] Колонка "День недели" заполнена корректно
- [ ] Все новые поля присутствуют
- [ ] Поле ID отсутствует
- [ ] Внизу файла итоговые суммы
- [ ] Суммы по типам оплаты корректны
- [ ] Раздельно "Работа" и "Наши детали"
- [ ] Файл корректно открывается в Excel на Windows
- [ ] Кириллица отображается без проблем

### Экспорт JSON
- [ ] Стандартный JSON работает
- [ ] Русский JSON создается корректно
- [ ] Поле ID удалено из экспорта
- [ ] Все новые поля присутствуют
- [ ] Русские ключи в русском JSON корректны

### Статистика
- [ ] Общая сумма отображается корректно
- [ ] Есть разбивка "Работа" с процентом
- [ ] Есть разбивка "Наши детали" с процентом
- [ ] В карточках типов оплаты показана дополнительная разбивка
- [ ] Проценты рассчитываются правильно

---

## 🧪 Сценарии ручного тестирования

### Тест 1: Создание нового заказа с разбивкой
**Шаги:**
1. Открыть вкладку "Заказ"
2. Заполнить:
   - Клиент: "Иван Петров"
   - Авто: "Lada Granta"
   - Работа: "Замена масла"
   - Сумма работы: 1500
   - Детали наши: "Масло Shell 5W-40"
   - Сумма деталей наших: 800
   - Детали клиента: "Фильтр воздушный"
   - Оплата: Наличные
3. Нажать "Сохранить заказ"

**Ожидаемый результат:**
- Заказ сохранен
- В истории отображается с итогом 2300₽
- В статистике учитывается раздельно: Работа 1500₽, Детали 800₽

### Тест 2: Календарь
**Шаги:**
1. Открыть вкладку "История"
2. Создать несколько заказов на разные даты
3. Нажать кнопку переключения на календарь
4. Проверить, что дни с заказами отмечены
5. Выбрать день с заказами

**Ожидаемый результат:**
- Календарь отображается
- Дни с заказами имеют точки
- При выборе дня показываются только заказы за этот день
- Можно листать месяцы

### Тест 3: Фильтрация по датам
**Шаги:**
1. Открыть вкладку "История"
2. Нажать кнопку фильтра (иконка календаря)
3. Ввести даты:
   - Начальная: 2025-01-01
   - Конечная: 2025-01-31
4. Нажать "Применить"

**Ожидаемый результат:**
- Показываются только заказы за январь 2025
- Виден индикатор фильтра с датами
- Можно сбросить фильтр

### Тест 4: Редактирование заказа
**Шаги:**
1. Открыть вкладку "История"
2. Выбрать любой заказ
3. Нажать кнопку "Изменить" (иконка карандаша)
4. Изменить сумму работы с 1500 на 2000
5. Изменить сумму деталей с 800 на 1000
6. Нажать "Сохранить изменения"

**Ожидаемый результат:**
- Форма редактирования открывается с заполненными данными
- Изменения сохраняются
- Итоговая сумма пересчитывается (3000₽)
- В истории отображается новая сумма

### Тест 5: Экспорт CSV и проверка в Excel
**Шаги:**
1. Создать 3-5 заказов с разными данными
2. Открыть "Настройки"
3. Нажать "Экспорт в CSV"
4. Сохранить файл
5. Открыть в Excel или Google Sheets

**Ожидаемый результат:**
- CSV файл скачивается
- Кириллица отображается корректно
- Все колонки присутствуют:
  - Дата, День недели, Клиент, Авто, Работы
  - Сумма работы, Детали наши, Сумма деталей наших
  - Детали клиента, Оплата, Итого
- Внизу итоговые суммы корректны
- Поле ID отсутствует

### Тест 6: Экспорт JSON (русский)
**Шаги:**
1. Создать несколько заказов
2. Открыть "Настройки"
3. Нажать "Экспорт в JSON (Русский)"
4. Открыть файл в текстовом редакторе

**Ожидаемый результат:**
- JSON файл создается
- Ключи на русском языке:
  - дата, день_недели, клиент, авто
  - работы_описание, сумма_работы
  - детали_наши, сумма_деталей_наших
  - детали_клиента, оплата, итого

### Тест 7: Миграция старых данных
**Шаги:**
1. Если есть старые данные с полями `amount` и `parts`
2. Запустить приложение
3. Открыть "История" и выбрать старый заказ

**Ожидаемый результат:**
- Старое `amount` преобразовано в `workAmount`
- Старое `parts` преобразовано в `clientParts`
- `ourPartsAmount` = 0
- `totalAmount` = workAmount
- Детали отображаются в правильных полях

---

## 🔧 Структура изменений

### Новые файлы:
- `components/OrderForm.js` - универсальный компонент формы

### Обновленные файлы:
- `lib/storage.js` - новые поля, миграция, updateOrder, экспорт
- `store/AppStore.js` - updateOrder, searchByDateRange
- `screens/HomeScreen.js` - использует OrderForm
- `screens/HistoryScreen.js` - календарь, редактирование, фильтр дат
- `screens/StatsScreen.js` - статистика по работе и деталям
- `screens/SettingsScreen.js` - русский JSON экспорт
- `utils/validators.js` - поддержка новых полей
- `package.json` - добавлен react-native-calendars

---

## 📦 Зависимости

Новая зависимость:
- `react-native-calendars` - календарь с русской локализацией

Все остальные зависимости остались без изменений.

---

## 🎯 Ключевые особенности реализации

1. **Обратная совместимость**: Старые данные автоматически мигрируются, старые поля сохранены для совместимости
2. **Единый компонент формы**: OrderForm используется для создания и редактирования
3. **Автоматический расчет**: totalAmount = workAmount + ourPartsAmount
4. **UTF-8 BOM в CSV**: Корректное отображение кириллицы в Excel/Android
5. **Календарь на русском**: Полная локализация календаря
6. **Множественные форматы экспорта**: JSON, JSON (русский), CSV

---

## ⚠️ Важные замечания

1. **Миграция данных**: Выполняется автоматически при первом запуске после обновления
2. **Поле amount**: Сохранено для совместимости со старым кодом, всегда равно totalAmount
3. **Валидация**: Требуется хотя бы одно из полей: workAmount или ourPartsAmount
4. **CSV экспорт**: Использует BOM для корректного отображения кириллицы
5. **Редактирование долгов**: При редактировании заказа-долга автоматически обновляется связанный долг

---

## 📱 Тестирование на устройствах

### Android:
```bash
npx expo start --android
```
- Проверить календарь
- Проверить CSV в приложениях для таблиц
- Проверить экспорт/импорт через Share

### iOS:
```bash
npx expo start --ios
```
- Проверить календарь
- Проверить CSV в Numbers
- Проверить экспорт/импорт через Share

### Web:
```bash
npx expo start --web
```
- Проверить все функции в браузере
- Проверить скачивание файлов

---

## 🎉 Готово!

Все функции из технического задания реализованы и готовы к тестированию.

Приятной работы с обновленным Master Journal V2! 🚀
